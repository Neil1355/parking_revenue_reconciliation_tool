"""
Minimal ingest tests placeholder.

Keeping tests small and deterministic so CI can run while implementations
are iterated on. Replace with full tests as modules mature.
"""

def test_placeholder():
    assert True
